upstream: https://github.com/Cubitect/cubiomes (active line: xpple/cubiomes master)
fork:     https://github.com/Conflux-Union/cubiomes (this project's fork)
commit:   f75f0a360e9ba3630478eecd0d5935f913e00ef9
commit date: 2026-09-18T20:38:49+08:00
pinned via: native/cubiomes git submodule (converted from a vendored copy on 2026-07-17)
license: MIT (see native/cubiomes/LICENSE)

Merge state: fork master = upstream xpple/master (5815e4f "Add 26.3 support
(#29)") merged on top of our feature line. Upstream changes folded in:
the renderpearl-independent C-side terrain refactor (terrainnoise/carver
split, uint8_t block columns), loot salt configs, the ctest suite, and
MC_26_3 (btree263 biome tree, abandoned camps).

Fork contents kept on top of upstream master:
  - 32a7299 "add 1.17 terrain feature queries" (this project's own addition)
  - da00a4a merge of upstream PR #162 (Zes-MinKey-Young), the MC_1_21_5
    biome tree. Verified before merging: over 20 seeds x 512x512 biome
    cells, the 1.21.4 and 1.21.5 generators differ in 0.011% of cells and
    every difference is dark_forest -> pale_garden, matching 25w02a.
  - 071ca34/e97dcf9/7580437 "modern overworld surfaces / surface columns /
    vegetation candidates" (this project's own additions) behind
    mapOverworldSurfaceColumns / mapOverworldSurfaceBiome /
    getChunkNaturalTreeCandidates.
  - Nether fossils: upstream 32f6624 now ships its own terrain-validated
    fossil model, so the fork keeps only what upstream lacks - the
    getFeaturePos routing in getStructurePos (upstream leaves fossils
    unplaced; the shim's structure catalog depends on the anchor-chunk
    contract) and the versioned anchor-biome rule in isViableStructurePos.
  - StructureType enum order follows upstream (Nether_Fossil=20,
    Abandoned_Camp=26, Stronghold=27); StructureIndex in the parent repo
    pins the same ordinals.

Verification for this pin (2026-09-18):
  - upstream ctest suite (test_biomes/structures/loot/pieces/carvers)
    passes, including the MC_26_3 cases
  - the fork's feature_tests harness passes
  - C smoke: MC_26_3 biome sampling, nether-fossil locate+viability, and
    abandoned-camp locate all work through the merged library
  - conflux-map :common:test (1068 tests incl. the 1.7-26.2 native
    oracle suite against this pin's prebuilt linux-x86_64 library) passes

Files compiled from this commit by buildNativesHost/buildNativesAll
(selected subset):
  biomenoise.c/h, biomes.c/h, generator.c/h, layers.c/h, noise.c/h,
  terrainnoise.c/h, finders.c/h, terrain_features.c/h, util.c/h, rng.h,
  features/end_city.c/h, features/fortress.c/h,
  features/abandoned_camp.c/h, features/stronghold.c/h,
  tables/btree18.h, tables/btree192.h, tables/btree19.h,
  tables/btree20.h, tables/btree21wd.h, tables/btree215.h,
  tables/btree262.h, tables/btree263.h

Not compiled (utilities/tests/docs present in the submodule but unused by
this project): quadbase.c/h, xradv.c, carver.c/h, tests.c, tests/,
feature_tests.c, loot/, docs/, CMakeLists.txt.
